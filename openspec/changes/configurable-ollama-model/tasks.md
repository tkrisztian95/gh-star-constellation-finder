## 1. Implementation

- [ ] 1.1 In `src/ai/ollamaAnalyzer.ts`, read `process.env.OLLAMA_MODEL ?? 'llama3'` inside `createOllamaAnalyzer` and use it as the model value instead of the hardcoded default parameter

## 2. Configuration

- [ ] 2.1 Add `OLLAMA_MODEL=llama3` entry to `.env.example`
