rearrange tui when user need to accept the suggestion for a repo the row showing the commands should be at the bottom and not above the section reviewing the repo anylsis
apply all suggestions key
default session save file name, enter save with that file name (auto generated bane), user must explicit skip , eg output folder, model name folder, etc and filename time stamp
improve headless mode so AI tools can also use this as a harness
Add or adjust help --help
cache and allow reuse analysis from previous runs 
possiblity run the app parallel headless compare results of different models
multi agent workflow analyse from different angle (frequently asked questions, security, features, issues, etc)